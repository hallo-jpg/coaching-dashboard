# Route Pacing System – Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Generisches GPX-basiertes Pacing-System: `generate_pacing.py` parst Routen aus `athlete/routes/`, berechnet Wattziele via Physikmodell + CP/W'-Logik und generiert `docs/pacing.html` mit Tab-Navigation. Coach-Skill gibt kompakten Pacing-Block in Rennwoche aus. Dashboard zeigt Karte T-14 bis T-0.

**Architecture:** `generate_pacing.py` läuft unabhängig von `generate.py`. Liest GPX-Dateien aus `athlete/routes/*.gpx`, liest Athleten-Parameter aus `athlete/profil.md` und `athlete/fortschritt.md`, rendert via `pacing.template.html` nach `docs/pacing.html`. Das Dashboard (`generate.py` + `dashboard.template.html`) bekommt eine neue kompakte `pacing_card` Komponente. Der Coach-Skill (`SKILL.md`) erhält einen Pacing-Block im Rennwoche-Modus.

**Tech Stack:** Python 3.11 · gpxpy · Jinja2 · SVG (inline) · GitHub Actions

---

## File Structure

| Datei | Status | Verantwortlichkeit |
|---|---|---|
| `athlete/routes/` | NEU (Ordner) | GPX-Routen + optionale JSON-Metadaten |
| `athlete/routes/archiv/` | NEU (Ordner) | Archivierte Routen |
| `generate_pacing.py` | NEU | GPX-Parser, Physikmodell, W', Intuitionstext, Renderer |
| `pacing.template.html` | NEU | Jinja2-Template für `docs/pacing.html` |
| `docs/pacing.html` | NEU (generiert) | Standalone Pacing-Tool, Tab-Navigation |
| `requirements.txt` | ÄNDERN | `gpxpy==1.6.2` hinzufügen |
| `generate.py` | ÄNDERN | `build_pacing_card()` + Kontext-Variable `pacing_card` |
| `dashboard.template.html` | ÄNDERN | Pacing-Karte Block (T-14 bis T-0) |
| `.claude/skills/coach/SKILL.md` | ÄNDERN | Pacing-Block in Rennwoche-Modus |
| `.github/workflows/build.yml` | ÄNDERN | `python generate_pacing.py` in Build-Pipeline |

---

## Task 1: Setup — Ordner, Abhängigkeit, Build-Pipeline

**Files:**
- Create: `athlete/routes/.gitkeep`
- Create: `athlete/routes/archiv/.gitkeep`
- Modify: `requirements.txt`
- Modify: `.github/workflows/build.yml`

- [ ] **Step 1: Ordner anlegen**

```bash
mkdir -p "athlete/routes/archiv"
touch "athlete/routes/.gitkeep"
touch "athlete/routes/archiv/.gitkeep"
```

- [ ] **Step 2: `gpxpy` zu requirements.txt hinzufügen**

Datei `requirements.txt` nach Edit:
```
requests==2.32.3
jinja2==3.1.4
pytest==8.3.4
gpxpy==1.6.2
```

- [ ] **Step 3: Lokale Installation testen**

```bash
pip install gpxpy==1.6.2
python -c "import gpxpy; print('gpxpy OK')"
```

Erwartet: `gpxpy OK`

- [ ] **Step 4: `build.yml` — `generate_pacing.py` nach `generate.py` einhängen**

Bestehenden `run: python generate.py` Block erweitern:

```yaml
      - name: Generate dashboard
        if: true
        env:
          INTERVALS_API_KEY:    ${{ secrets.INTERVALS_API_KEY }}
          INTERVALS_ATHLETE_ID: ${{ secrets.INTERVALS_ATHLETE_ID }}
          TZ: Europe/Berlin
        run: |
          python generate.py
          python generate_pacing.py
```

`git add docs/` im Commit-Schritt erfasst `docs/pacing.html` automatisch.

- [ ] **Step 5: Commit**

```bash
git add athlete/routes/ requirements.txt .github/workflows/build.yml
git commit -m "feat: setup pacing system — routes dir, gpxpy, build pipeline"
```

---

## Task 2: GPX-Parser

**Files:**
- Create: `generate_pacing.py` (Datei anlegen, erste Funktion)
- Create: `tests/test_pacing.py` (Test-Datei anlegen)
- Create: `tests/fixtures/mini.gpx` (Minimal-GPX zum Testen)

- [ ] **Step 1: Minimal-GPX Fixture erstellen**

`tests/fixtures/mini.gpx` — 4 Punkte, ~1km, 50Hm:
```xml
<?xml version="1.0"?>
<gpx version="1.1" creator="test">
  <trk><trkseg>
    <trkpt lat="47.500" lon="10.500"><ele>900</ele></trkpt>
    <trkpt lat="47.504" lon="10.502"><ele>920</ele></trkpt>
    <trkpt lat="47.508" lon="10.504"><ele>940</ele></trkpt>
    <trkpt lat="47.512" lon="10.506"><ele>950</ele></trkpt>
  </trkseg></trk>
</gpx>
```

- [ ] **Step 2: Failing Test schreiben**

`tests/test_pacing.py`:
```python
import sys, os
sys.path.insert(0, os.path.dirname(os.path.dirname(__file__)))
from generate_pacing import parse_gpx, group_segments

FIXTURE = "tests/fixtures/mini.gpx"

def test_parse_gpx_returns_points():
    pts = parse_gpx(FIXTURE)
    assert len(pts) >= 2
    assert "dist" in pts[0]
    assert "ele" in pts[0]

def test_group_segments_returns_segments():
    pts = parse_gpx(FIXTURE)
    segs = group_segments(pts)
    assert len(segs) >= 1
    assert "dist_m" in segs[0]
    assert "gradient_pct" in segs[0]
    assert "elev_start" in segs[0]
    assert "elev_end" in segs[0]

def test_total_distance_plausible():
    pts = parse_gpx(FIXTURE)
    total = pts[-1]["dist"]
    assert 500 < total < 2000  # mini.gpx ist ~1km

def test_gradient_direction():
    pts = parse_gpx(FIXTURE)
    segs = group_segments(pts)
    # mini.gpx steigt → alle Gradienten positiv
    for s in segs:
        assert s["gradient_pct"] > 0
```

- [ ] **Step 3: Test zum Scheitern bringen**

```bash
pytest tests/test_pacing.py -v
```

Erwartet: `ImportError: cannot import name 'parse_gpx'`

- [ ] **Step 4: `generate_pacing.py` anlegen mit `parse_gpx` + `group_segments`**

```python
#!/usr/bin/env python3
"""Route Pacing System — GPX Parser, Physics Model, HTML Renderer."""
from __future__ import annotations
import json
import math
import re
from datetime import date, datetime
from pathlib import Path

import gpxpy
from jinja2 import Environment, FileSystemLoader


# ── GPX Parser ────────────────────────────────────────────────────────────────

def _haversine_m(lat1: float, lon1: float, lat2: float, lon2: float) -> float:
    """Distance in metres between two lat/lon points."""
    R = 6_371_000
    φ1, φ2 = math.radians(lat1), math.radians(lat2)
    dφ = math.radians(lat2 - lat1)
    dλ = math.radians(lon2 - lon1)
    a = math.sin(dφ / 2) ** 2 + math.cos(φ1) * math.cos(φ2) * math.sin(dλ / 2) ** 2
    return 2 * R * math.asin(math.sqrt(a))


def parse_gpx(path: str) -> list[dict]:
    """Parse GPX file → list of {"dist": float, "ele": float} points at 50m intervals."""
    with open(path, encoding="utf-8") as f:
        gpx = gpxpy.parse(f)

    raw: list[dict] = []
    for track in gpx.tracks:
        for segment in track.segments:
            for pt in segment.points:
                raw.append({"lat": pt.latitude, "lon": pt.longitude, "ele": pt.elevation or 0.0})

    if len(raw) < 2:
        return []

    # Cumulative distance
    cum = 0.0
    pts: list[dict] = [{"dist": 0.0, "ele": raw[0]["ele"]}]
    for i in range(1, len(raw)):
        cum += _haversine_m(raw[i - 1]["lat"], raw[i - 1]["lon"], raw[i]["lat"], raw[i]["lon"])
        pts.append({"dist": cum, "ele": raw[i]["ele"]})

    # Resample at 50m intervals via linear interpolation
    INTERVAL = 50.0
    total = pts[-1]["dist"]
    resampled: list[dict] = []
    d = 0.0
    j = 0
    while d <= total:
        while j < len(pts) - 2 and pts[j + 1]["dist"] < d:
            j += 1
        seg_len = pts[j + 1]["dist"] - pts[j]["dist"]
        frac = (d - pts[j]["dist"]) / seg_len if seg_len > 0 else 0.0
        ele = pts[j]["ele"] + frac * (pts[j + 1]["ele"] - pts[j]["ele"])
        resampled.append({"dist": round(d, 1), "ele": round(ele, 1)})
        d += INTERVAL

    return resampled


def group_segments(pts: list[dict], min_dist_m: float = 300.0, grad_threshold: float = 1.5) -> list[dict]:
    """Group resampled points into meaningful segments.

    A new segment starts when gradient changes by >grad_threshold % OR
    after min_dist_m metres, whichever gives better readability (4–8 segs per route).
    """
    if len(pts) < 2:
        return []

    segments: list[dict] = []
    start = pts[0]

    for i in range(1, len(pts)):
        dist_m = pts[i]["dist"] - start["dist"]
        if dist_m < min_dist_m:
            continue

        grad = (pts[i]["ele"] - start["ele"]) / dist_m * 100

        # Check if gradient changed significantly vs previous segment
        prev_grad = segments[-1]["gradient_pct"] if segments else None
        new_seg = prev_grad is None or abs(grad - prev_grad) >= grad_threshold or dist_m >= 1200

        if new_seg:
            segments.append({
                "dist_m": round(dist_m, 1),
                "elev_start": round(start["ele"], 1),
                "elev_end": round(pts[i]["ele"], 1),
                "gradient_pct": round(grad, 2),
            })
            start = pts[i]

    # Last partial segment
    if pts[-1]["dist"] > start["dist"]:
        dist_m = pts[-1]["dist"] - start["dist"]
        if dist_m > 50:
            grad = (pts[-1]["ele"] - start["ele"]) / dist_m * 100
            segments.append({
                "dist_m": round(dist_m, 1),
                "elev_start": round(start["ele"], 1),
                "elev_end": round(pts[-1]["ele"], 1),
                "gradient_pct": round(grad, 2),
            })

    return segments
```

- [ ] **Step 5: Tests laufen lassen**

```bash
pytest tests/test_pacing.py -v
```

Erwartet: 4 PASSED

- [ ] **Step 6: Commit**

```bash
git add generate_pacing.py tests/
git commit -m "feat: GPX parser mit 50m-Resampling und Segment-Gruppierung"
```

---

## Task 3: Physikmodell — Velocity Solver + Power Assignment + W'-Balance

**Files:**
- Modify: `generate_pacing.py` (neue Funktionen anhängen)
- Modify: `tests/test_pacing.py` (neue Tests)

- [ ] **Step 1: Failing Tests schreiben**

An `tests/test_pacing.py` anhängen:
```python
from generate_pacing import velocity_from_power, assign_power, compute_w_prime_balance

def test_velocity_increases_with_power():
    v1 = velocity_from_power(200, 5.0, "climb")
    v2 = velocity_from_power(300, 5.0, "climb")
    assert v2 > v1

def test_velocity_decreases_with_gradient():
    v_flat = velocity_from_power(250, 1.0, "climb")
    v_steep = velocity_from_power(250, 8.0, "climb")
    assert v_flat > v_steep

def test_velocity_reasonable_range():
    # 300W auf 5% Anstieg → ca. 12–18 km/h
    v = velocity_from_power(300, 5.0, "climb")
    assert 3.0 < v < 6.0  # m/s

def test_assign_power_increases_with_gradient():
    p_flat = assign_power(2.0, "climb", 300)
    p_steep = assign_power(7.0, "climb", 300)
    assert p_steep > p_flat

def test_assign_power_tt_higher_than_climb():
    p_climb = assign_power(5.0, "climb", 300)
    p_tt = assign_power(5.0, "tt", 300)
    assert p_tt > p_climb

def test_w_prime_depletes_on_hard_segments():
    segs = [
        {"target_w": 350, "time_s": 300},  # above CP=300 → depletes
        {"target_w": 200, "time_s": 300},  # below CP → recovers
    ]
    result = compute_w_prime_balance(segs, cp_w=300, w_prime_total_j=20000)
    # After seg 1: balance < 20000
    assert result[0]["w_prime_balance_j"] < 20000
    # After seg 2 (recovery): balance higher than after seg 1
    assert result[1]["w_prime_balance_j"] > result[0]["w_prime_balance_j"]

def test_w_prime_never_exceeds_total():
    segs = [{"target_w": 100, "time_s": 600}]  # far below CP → recovery
    result = compute_w_prime_balance(segs, cp_w=300, w_prime_total_j=20000)
    assert result[0]["w_prime_balance_j"] <= 20000
```

- [ ] **Step 2: Test zum Scheitern bringen**

```bash
pytest tests/test_pacing.py -v
```

Erwartet: `ImportError: cannot import name 'velocity_from_power'`

- [ ] **Step 3: Funktionen in `generate_pacing.py` implementieren**

Nach den GPX-Funktionen anhängen:

```python
# ── Physics Model ─────────────────────────────────────────────────────────────

_PHYSICS = {
    "mass_kg":   96.0,   # Fahrer (88kg) + Rad (8kg)
    "g":          9.81,
    "Cr":         0.004,  # Rollwiderstand Asphalt
    "rho":        1.20,   # Luftdichte Meereshöhe
    "CdA_road":   0.32,   # Aufsitzposition Rennrad
    "CdA_tt":     0.26,   # TT-Position
}

_POWER_TABLE = [
    (0.0,          0.60),  # Abfahrt (<0%): 60% CP Erholung
    (2.0,          0.92),  # 0–2%: 92% CP
    (4.0,          0.97),  # 2–4%: 97% CP
    (6.0,          1.00),  # 4–6%: 100% CP
    (8.0,          1.05),  # 6–8%: 105% CP (W' Einsatz)
    (float("inf"), 1.08),  # >8%: 108% CP (W' aggressiv)
]

_TYPE_FACTOR = {
    "tt":          1.03,
    "climb":       1.00,
    "gran_fondo":  0.95,
}


def velocity_from_power(power_w: float, gradient_pct: float, route_type: str = "climb") -> float:
    """Löse Geschwindigkeit aus Zielleistung und Gradient (Newton-Verfahren).

    P = m·g·v·sin(arctan(g%/100)) + m·g·v·Cr·cos(θ) + 0.5·ρ·CdA·v³
    """
    m   = _PHYSICS["mass_kg"]
    g   = _PHYSICS["g"]
    Cr  = _PHYSICS["Cr"]
    rho = _PHYSICS["rho"]
    CdA = _PHYSICS["CdA_tt"] if route_type == "tt" else _PHYSICS["CdA_road"]

    theta     = math.atan(gradient_pct / 100)
    F_gravity = m * g * math.sin(theta)
    F_roll    = m * g * Cr * math.cos(theta)
    A_lin     = F_gravity + F_roll   # linearer Widerstandsterm
    A_cube    = 0.5 * rho * CdA      # kubischer Aerowiderstand

    # Newton: f(v) = A_cube·v³ + A_lin·v - P = 0
    v = max(power_w / max(A_lin + 1, 0.01), 0.5)
    for _ in range(30):
        fv  = A_cube * v ** 3 + A_lin * v - power_w
        dfv = 3 * A_cube * v ** 2 + A_lin
        if abs(dfv) < 1e-12:
            break
        delta = fv / dfv
        v = max(v - delta, 0.1)
        if abs(delta) < 1e-7:
            break
    return round(v, 4)


def assign_power(gradient_pct: float, route_type: str, cp_w: float) -> float:
    """Gibt Zielwatt für ein Segment zurück (Gradient-Tabelle × Typ-Faktor × CP)."""
    if gradient_pct < 0:
        return round(cp_w * 0.60)
    factor = _TYPE_FACTOR.get(route_type, 1.0)
    for threshold, pct in _POWER_TABLE:
        if gradient_pct < threshold:
            return round(cp_w * pct * factor)
    return round(cp_w * 1.08 * factor)


def compute_w_prime_balance(
    segments: list[dict],
    cp_w: float,
    w_prime_total_j: float,
    k_recovery: float = 0.35,
) -> list[dict]:
    """Berechnet W'-Balance durch die Route.

    Jedes Segment bekommt "w_prime_balance_j" (verbleibende W' in Joule).
    k_recovery: empirischer Faktor für W'-Rekonstituierung unter CP.
    """
    balance = w_prime_total_j
    result = []
    for seg in segments:
        power = seg["target_w"]
        time_s = seg["time_s"]
        if power > cp_w:
            balance -= (power - cp_w) * time_s        # Verbrauch
        else:
            balance += k_recovery * (cp_w - power) * time_s  # Erholung
        balance = min(balance, w_prime_total_j)        # Nie über Maximum
        balance = max(balance, 0.0)                    # Nie negativ
        result.append({**seg, "w_prime_balance_j": round(balance, 1)})
    return result
```

- [ ] **Step 4: Tests laufen lassen**

```bash
pytest tests/test_pacing.py -v
```

Erwartet: alle PASSED

- [ ] **Step 5: Commit**

```bash
git add generate_pacing.py tests/test_pacing.py
git commit -m "feat: Physikmodell — velocity solver, power assignment, W' balance"
```

---

## Task 4: Athleten-Daten-Reader + Kontext-Builder

**Files:**
- Modify: `generate_pacing.py`
- Modify: `tests/test_pacing.py`

- [ ] **Step 1: Failing Tests**

An `tests/test_pacing.py` anhängen:
```python
from generate_pacing import read_athlete_params, load_route_meta, compute_route

def test_read_athlete_params_returns_required_keys():
    params = read_athlete_params()
    assert "ftp_w" in params
    assert "cp_w" in params
    assert "w_prime_j" in params
    assert params["ftp_w"] > 100
    assert params["cp_w"] > 100
    assert params["w_prime_j"] > 1000

def test_load_route_meta_defaults():
    meta = load_route_meta("nonexistent_route")
    assert meta["type"] == "climb"
    assert meta["target_if"] == 0.95

def test_compute_route_produces_segments():
    pts = parse_gpx(FIXTURE)
    segs = group_segments(pts)
    params = read_athlete_params()
    result = compute_route(segs, params, route_type="climb")
    assert len(result) >= 1
    assert "target_w" in result[0]
    assert "time_s" in result[0]
    assert "speed_kmh" in result[0]
    assert "w_prime_balance_j" in result[0]
    assert "km_start" in result[0]
    assert "km_end" in result[0]
```

- [ ] **Step 2: Test zum Scheitern bringen**

```bash
pytest tests/test_pacing.py::test_read_athlete_params_returns_required_keys -v
```

Erwartet: `ImportError: cannot import name 'read_athlete_params'`

- [ ] **Step 3: Funktionen implementieren**

An `generate_pacing.py` anhängen:

```python
# ── Athlete Data Reader ───────────────────────────────────────────────────────

def read_athlete_params() -> dict:
    """Liest FTP, CP, W' aus athlete/profil.md und athlete/fortschritt.md.

    Fallback-Werte wenn Parsing fehlschlägt.
    """
    params = {"ftp_w": 305, "cp_w": 305, "w_prime_j": 28_100}

    # FTP aus profil.md
    profil = Path("athlete/profil.md")
    if profil.exists():
        text = profil.read_text(encoding="utf-8")
        m = re.search(r"\*\*FTP\*\*\s*\|\s*\*\*(\d+)W\*\*", text)
        if m:
            params["ftp_w"] = int(m.group(1))

    # CP + W' aus fortschritt.md
    fortschritt = Path("athlete/fortschritt.md")
    if fortschritt.exists():
        text = fortschritt.read_text(encoding="utf-8")
        # CP aus CP/W'-Verlauf Tabelle: "| 04.04.2026 | **305W** | **28,1 kJ** |"
        m_cp = re.search(r"\|\s*\*\*(\d+)W\*\*\s*\|\s*\*\*([\d,]+)\s*kJ\*\*", text)
        if m_cp:
            params["cp_w"] = int(m_cp.group(1))
            params["w_prime_j"] = int(float(m_cp.group(2).replace(",", ".")) * 1000)

    return params


def load_route_meta(gpx_path: str) -> dict:
    """Liest optionale JSON-Metadaten neben der GPX-Datei.

    Falls keine JSON vorhanden: sinnvolle Defaults zurückgeben.
    """
    defaults = {
        "name": Path(gpx_path).stem.replace("-", " ").replace("_", " ").title(),
        "type": "climb",
        "event_date": None,
        "target_if": 0.95,
        "notes": "",
    }
    meta_path = Path(gpx_path).with_suffix(".json")
    if not meta_path.exists():
        return defaults
    try:
        data = json.loads(meta_path.read_text(encoding="utf-8"))
        return {**defaults, **data}
    except (json.JSONDecodeError, OSError):
        return defaults


def compute_route(
    segments: list[dict],
    athlete: dict,
    route_type: str = "climb",
) -> list[dict]:
    """Kombiniert alle Engine-Schritte zu vollständigen Segment-Dicts.

    Jedes Segment enthält: target_w, speed_kmh, time_s, km_start, km_end,
    w_prime_balance_j, gradient_pct, zone_label, zone_color.
    """
    cp = athlete["cp_w"]
    w_prime = athlete["w_prime_j"]

    cum_dist = 0.0
    enriched = []
    for seg in segments:
        power = assign_power(seg["gradient_pct"], route_type, cp)
        v_ms  = velocity_from_power(power, seg["gradient_pct"], route_type)
        time_s = seg["dist_m"] / max(v_ms, 0.1)

        # Zone-Label + Farbe für Template
        pct_cp = power / cp
        if pct_cp < 0.65:
            zone_label, zone_color = "Erholung", "#4ade80"
        elif pct_cp < 0.97:
            zone_label, zone_color = "Aerob", "#4ade80"
        elif pct_cp < 1.03:
            zone_label, zone_color = "MIT", "#facc15"
        elif pct_cp < 1.08:
            zone_label, zone_color = "MIT+", "#f97316"
        else:
            zone_label, zone_color = "HIT", "#ef4444"

        km_start = round(cum_dist / 1000, 2)
        cum_dist += seg["dist_m"]
        km_end   = round(cum_dist / 1000, 2)

        enriched.append({
            **seg,
            "target_w":   power,
            "speed_kmh":  round(v_ms * 3.6, 1),
            "time_s":     round(time_s, 1),
            "km_start":   km_start,
            "km_end":     km_end,
            "km_label":   f"km {km_start:.1f}–{km_end:.1f}",
            "zone_label": zone_label,
            "zone_color": zone_color,
        })

    return compute_w_prime_balance(enriched, cp, w_prime)
```

- [ ] **Step 4: Tests laufen lassen**

```bash
pytest tests/test_pacing.py -v
```

Erwartet: alle PASSED

- [ ] **Step 5: Commit**

```bash
git add generate_pacing.py tests/test_pacing.py
git commit -m "feat: Athleten-Reader, Route-Meta-Loader, compute_route Integrationsfunktion"
```

---

## Task 5: Intuitionstext + Pacing-Kontext-Builder

**Files:**
- Modify: `generate_pacing.py`
- Modify: `tests/test_pacing.py`

- [ ] **Step 1: Failing Tests**

An `tests/test_pacing.py` anhängen:
```python
from generate_pacing import generate_intuition, build_route_context

def test_generate_intuition_returns_list_of_strings():
    pts = parse_gpx(FIXTURE)
    segs = group_segments(pts)
    params = read_athlete_params()
    computed = compute_route(segs, params, "climb")
    tips = generate_intuition(computed, params)
    assert isinstance(tips, list)
    assert len(tips) >= 2
    for t in tips:
        assert isinstance(t, str)
        assert len(t) > 10

def test_build_route_context_keys():
    ctx = build_route_context(FIXTURE)
    assert "name" in ctx
    assert "segments" in ctx
    assert "total_time_s" in ctx
    assert "total_dist_km" in ctx
    assert "avg_power_w" in ctx
    assert "w_prime_spent_kj" in ctx
    assert "w_prime_remaining_kj" in ctx
    assert "intuition" in ctx
    assert "svg_profile" in ctx
```

- [ ] **Step 2: Test zum Scheitern bringen**

```bash
pytest tests/test_pacing.py::test_generate_intuition_returns_list_of_strings -v
```

Erwartet: `ImportError: cannot import name 'generate_intuition'`

- [ ] **Step 3: Implementierung**

An `generate_pacing.py` anhängen:

```python
# ── Intuition Text ────────────────────────────────────────────────────────────

def generate_intuition(segments: list[dict], athlete: dict) -> list[str]:
    """Generiert 3–4 coaching-style Sätze die erklären was die Zahlen bedeuten."""
    cp  = athlete["cp_w"]
    w_total_kj = athlete["w_prime_j"] / 1000
    tips: list[str] = []

    if not segments:
        return ["Keine Segmentdaten verfügbar."]

    first = segments[0]
    tips.append(
        f"Start ({first['km_label']}): {first['target_w']}W fühlt sich zu leicht an — das ist korrekt. "
        f"Bei richtigem Pacing setzt das Belastungsgefühl erst nach 3–5 Min ein. Wenn es schon am "
        f"Start schwer wirkt, bist du zu schnell."
    )

    # Steilstes Segment (>4% Gradient)
    hard_segs = [s for s in segments if s["gradient_pct"] > 4]
    if hard_segs:
        steepest = max(hard_segs, key=lambda s: s["gradient_pct"])
        min_dur  = int(steepest["time_s"] / 60)
        tips.append(
            f"Steilstück {steepest['km_label']} ({steepest['gradient_pct']:.1f}%): "
            f"{steepest['target_w']}W für ~{min_dur} Min — das entspricht von der Intensität "
            f"deiner MIT-Schwellenarbeit vom Training. Dieses Gefühl kennst du. Hier ist es wichtig, "
            f"nicht zu überziehen: über {steepest['target_w'] + 15}W kosten unverhältnismäßig viel W'."
        )

    # W' Budget
    w_spent  = (athlete["w_prime_j"] - segments[-1]["w_prime_balance_j"]) / 1000
    w_remain = segments[-1]["w_prime_balance_j"] / 1000
    if w_remain < 5:
        budget_comment = "sehr knapp — kein Spielraum. Pacing exakt einhalten."
    elif w_remain < 10:
        budget_comment = "ausreichend für kleinere Tempoanpassungen."
    else:
        budget_comment = "komfortabel — du kannst auf unerwartete Steilpassagen reagieren."
    tips.append(
        f"W'-Budget: {w_spent:.1f} von {w_total_kj:.1f} kJ verplant, "
        f"{w_remain:.1f} kJ Reserve — {budget_comment}"
    )

    tips.append(
        "Herzrate ignorieren: Bei dieser Belastungsdauer steigt die HR kontinuierlich bis zum Ziel. "
        "Watt ist der Anker — HR ist nur Kontrollgröße, kein Steuerungsinstrument."
    )
    return tips


# ── SVG Profile Generator ─────────────────────────────────────────────────────

def _build_svg_profile(segments: list[dict], width: int = 800, height: int = 160) -> str:
    """Erzeugt SVG-Höhenprofil als HTML-String.

    Text-Labels werden AUSSERHALB des skalierten SVG-Bereichs gerendert
    (kein preserveAspectRatio="none") um Verzerrung zu vermeiden.
    """
    if not segments:
        return ""

    total_dist = sum(s["dist_m"] for s in segments)
    all_eles   = [s["elev_start"] for s in segments] + [segments[-1]["elev_end"]]
    ele_min    = min(all_eles)
    ele_max    = max(all_eles)
    ele_range  = max(ele_max - ele_min, 1)

    PAD_TOP, PAD_BOT = 30, 20  # pixels reserved for labels above/below
    chart_h = height - PAD_TOP - PAD_BOT

    def x(dist_m: float) -> float:
        return dist_m / total_dist * width

    def y(ele: float) -> float:
        return PAD_TOP + chart_h * (1 - (ele - ele_min) / ele_range)

    # Build points for elevation polyline
    pts_coords: list[tuple[float, float]] = []
    cum = 0.0
    for seg in segments:
        pts_coords.append((x(cum), y(seg["elev_start"])))
        cum += seg["dist_m"]
    pts_coords.append((x(cum), y(segments[-1]["elev_end"])))

    poly_pts = " ".join(f"{px:.1f},{py:.1f}" for px, py in pts_coords)
    fill_pts = poly_pts + f" {width:.1f},{height} 0,{height}"

    lines: list[str] = [
        f'<svg width="100%" height="{height}" viewBox="0 0 {width} {height}" '
        f'style="display:block;border-radius:8px;overflow:visible">',
        "<defs>",
    ]

    # Gradient fills per segment
    cum = 0.0
    for i, seg in enumerate(segments):
        color = seg["zone_color"]
        x1 = x(cum)
        x2 = x(cum + seg["dist_m"])
        lines.append(
            f'<rect x="{x1:.1f}" y="{PAD_TOP}" width="{x2-x1:.1f}" height="{chart_h}" '
            f'fill="{color}" fill-opacity="0.18"/>'
        )
        cum += seg["dist_m"]

    lines.append("</defs>")

    # Elevation fill + line
    lines.append(f'<polygon points="{fill_pts}" fill="rgba(255,255,255,0.04)"/>')
    lines.append(
        f'<polyline points="{poly_pts}" fill="none" stroke="#e2e8f0" '
        f'stroke-width="2.5" stroke-linejoin="round" stroke-linecap="round"/>'
    )

    # Labels per segment (text outside scaled area → no distortion)
    cum = 0.0
    for seg in segments:
        cx = x(cum + seg["dist_m"] / 2)
        color = seg["zone_color"]
        lines.append(
            f'<text x="{cx:.1f}" y="{PAD_TOP - 12}" text-anchor="middle" '
            f'fill="{color}" font-size="12" font-weight="700" font-family="sans-serif">'
            f'{seg["gradient_pct"]:.1f}%</text>'
        )
        lines.append(
            f'<text x="{cx:.1f}" y="{PAD_TOP - 0}" text-anchor="middle" '
            f'fill="{color}" font-size="11" font-family="sans-serif">'
            f'{seg["target_w"]}W</text>'
        )
        # Distance marker at bottom
        x_start = x(cum)
        lines.append(
            f'<text x="{x_start:.1f}" y="{height}" text-anchor="middle" '
            f'fill="#475569" font-size="10" font-family="sans-serif">'
            f'{seg["km_start"]:.1f}</text>'
        )
        cum += seg["dist_m"]

    # Final distance marker
    lines.append(
        f'<text x="{width}" y="{height}" text-anchor="end" '
        f'fill="#475569" font-size="10" font-family="sans-serif">'
        f'{segments[-1]["km_end"]:.1f} km</text>'
    )

    lines.append("</svg>")
    return "\n".join(lines)


# ── Route Context Builder ─────────────────────────────────────────────────────

def build_route_context(gpx_path: str) -> dict:
    """Vollständiger Kontext-Dict für Jinja2-Template aus einer GPX-Datei."""
    meta    = load_route_meta(gpx_path)
    athlete = read_athlete_params()
    pts     = parse_gpx(gpx_path)
    segs_raw = group_segments(pts)
    segments = compute_route(segs_raw, athlete, route_type=meta["type"])

    total_time_s  = sum(s["time_s"] for s in segments)
    total_dist_m  = sum(s["dist_m"] for s in segments)
    total_ele     = sum(max(s["elev_end"] - s["elev_start"], 0) for s in segments)
    avg_power     = (sum(s["target_w"] * s["time_s"] for s in segments)
                     / max(total_time_s, 1))
    w_spent_kj    = (athlete["w_prime_j"] - segments[-1]["w_prime_balance_j"]) / 1000 if segments else 0
    w_remain_kj   = segments[-1]["w_prime_balance_j"] / 1000 if segments else athlete["w_prime_j"] / 1000

    # Time formatting helpers
    def fmt_time(secs: float) -> str:
        m, s = divmod(int(secs), 60)
        return f"{m}:{s:02d}"

    def fmt_split(secs: float) -> str:
        m, s = divmod(int(secs), 60)
        return f"{m}:{s:02d}"

    # Range ±3% / +5%
    t_low  = fmt_time(total_time_s * 0.97)
    t_high = fmt_time(total_time_s * 1.05)

    # Event date display
    event_date_str = ""
    if meta.get("event_date"):
        try:
            d = date.fromisoformat(meta["event_date"])
            event_date_str = d.strftime("%-d. %B %Y")
        except ValueError:
            event_date_str = meta["event_date"]

    for seg in segments:
        seg["time_fmt"] = fmt_split(seg["time_s"])

    return {
        "name":               meta["name"],
        "route_type":         meta["type"],
        "event_date":         event_date_str,
        "notes":              meta.get("notes", ""),
        "segments":           segments,
        "total_time_s":       round(total_time_s),
        "total_time_fmt":     fmt_time(total_time_s),
        "total_time_range":   f"{t_low}–{t_high}",
        "total_dist_km":      round(total_dist_m / 1000, 2),
        "total_ele_m":        round(total_ele),
        "avg_power_w":        round(avg_power),
        "avg_if":             round(avg_power / max(athlete["ftp_w"], 1), 2),
        "w_prime_spent_kj":   round(w_spent_kj, 1),
        "w_prime_remaining_kj": round(w_remain_kj, 1),
        "w_prime_total_kj":   round(athlete["w_prime_j"] / 1000, 1),
        "w_prime_pct_used":   round(w_spent_kj / max(athlete["w_prime_j"] / 1000, 1) * 100),
        "intuition":          generate_intuition(segments, athlete),
        "svg_profile":        _build_svg_profile(segments),
        "athlete":            athlete,
    }
```

- [ ] **Step 4: Tests laufen lassen**

```bash
pytest tests/test_pacing.py -v
```

Erwartet: alle PASSED

- [ ] **Step 5: Commit**

```bash
git add generate_pacing.py tests/test_pacing.py
git commit -m "feat: Intuitionstext, SVG-Profil-Generator, build_route_context"
```

---

## Task 6: HTML-Template (`pacing.template.html`)

**Files:**
- Create: `pacing.template.html`

Kein Test (rein visuell). Struktur folgt `dashboard.template.html` Muster.

- [ ] **Step 1: `pacing.template.html` erstellen**

```html
<!DOCTYPE html>
<html lang="de">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Race Pacing</title>
<style>
  * { box-sizing: border-box; margin: 0; padding: 0; }
  body { background: #0d1117; color: #e2e8f0; font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif; padding: 0; }

  /* Tab navigation */
  .tab-bar { display: flex; gap: 0; background: #0d1117; border-bottom: 1px solid #1e293b; padding: 0 24px; overflow-x: auto; }
  .tab { padding: 12px 18px; font-size: 0.75rem; font-weight: 700; color: #64748b; cursor: pointer; border-bottom: 2px solid transparent; white-space: nowrap; }
  .tab.active { color: #60a5fa; border-bottom-color: #60a5fa; }
  .tab-content { display: none; padding: 24px; }
  .tab-content.active { display: block; }

  /* Header */
  .route-header { display: flex; align-items: baseline; gap: 12px; margin-bottom: 20px; flex-wrap: wrap; }
  .route-header h1 { font-size: 1.3rem; font-weight: 800; }
  .pill { background: rgba(96,165,250,0.15); color: #60a5fa; font-size: 0.65rem; font-weight: 700; padding: 3px 10px; border-radius: 99px; }

  /* KPI grid */
  .kpi-row { display: grid; grid-template-columns: repeat(4, 1fr); gap: 12px; margin-bottom: 20px; }
  @media (max-width: 600px) { .kpi-row { grid-template-columns: repeat(2, 1fr); } }
  .kpi { background: #161d2a; border: 1px solid #1e293b; border-radius: 10px; padding: 14px 16px; }
  .kpi-label { font-size: 0.62rem; color: #64748b; text-transform: uppercase; letter-spacing: 0.05em; margin-bottom: 4px; }
  .kpi-value { font-size: 1.5rem; font-weight: 800; line-height: 1; }
  .kpi-sub { font-size: 0.65rem; color: #64748b; margin-top: 4px; }

  /* Cards */
  .card { background: #161d2a; border: 1px solid #1e293b; border-radius: 12px; padding: 20px; margin-bottom: 16px; }
  .card-title { font-size: 0.72rem; font-weight: 700; color: #94a3b8; text-transform: uppercase; letter-spacing: 0.06em; margin-bottom: 16px; }

  /* W' meter */
  .w-bar-wrap { display: flex; align-items: center; gap: 12px; margin-bottom: 16px; }
  .w-bar-track { flex: 1; height: 10px; background: #1e293b; border-radius: 5px; overflow: hidden; }
  .w-bar-fill { height: 100%; border-radius: 5px; background: linear-gradient(90deg, #4ade80 0%, #facc15 60%, #f97316 85%, #ef4444 100%); }

  /* Segment table */
  .seg-table { width: 100%; border-collapse: collapse; }
  .seg-table th { font-size: 0.65rem; color: #64748b; text-transform: uppercase; letter-spacing: 0.04em; padding: 6px 12px; text-align: left; border-bottom: 1px solid #1e293b; }
  .seg-table th:not(:first-child) { text-align: right; }
  .seg-table td { padding: 10px 12px; font-size: 0.8rem; border-bottom: 1px solid #0d1117; vertical-align: top; }
  .seg-table td:not(:first-child) { text-align: right; }
  .seg-table tr:last-child td { border-bottom: none; background: rgba(96,165,250,0.06); font-weight: 700; color: #60a5fa; }
  .seg-name { font-weight: 600; }
  .seg-sub { font-size: 0.62rem; color: #64748b; margin-top: 2px; }
  .badge { display: inline-block; font-size: 0.6rem; font-weight: 700; padding: 2px 7px; border-radius: 99px; }

  /* Intuition section */
  .tips { display: flex; flex-direction: column; gap: 10px; }
  .tip { display: flex; gap: 10px; align-items: flex-start; padding: 10px 14px; background: #0d1117; border-radius: 8px; }
  .tip-icon { font-size: 1rem; flex-shrink: 0; line-height: 1.4; }
  .tip-text { font-size: 0.78rem; color: #94a3b8; line-height: 1.6; }

  /* Two-col layout */
  .two-col { display: grid; grid-template-columns: 1fr 1fr; gap: 16px; }
  @media (max-width: 700px) { .two-col { grid-template-columns: 1fr; } }

  .note { font-size: 0.62rem; color: #334155; margin-top: 10px; }
</style>
</head>
<body>

<div class="tab-bar">
{% for route in routes %}
  <div class="tab {% if loop.first %}active{% endif %}" onclick="showTab('{{ loop.index0 }}')">
    {{ route.name }}{% if route.event_date %} · {{ route.event_date }}{% endif %}
  </div>
{% endfor %}
</div>

{% for route in routes %}
<div class="tab-content {% if loop.first %}active{% endif %}" id="tab-{{ loop.index0 }}">

  <div class="route-header">
    <h1>⚡ {{ route.name }}</h1>
    {% if route.event_date %}<span class="pill">{{ route.event_date }}</span>{% endif %}
    <span class="pill">{{ route.total_dist_km }} km · {{ route.total_ele_m }} Hm</span>
    <span class="pill">FTP {{ route.athlete.ftp_w }}W · CP {{ route.athlete.cp_w }}W</span>
  </div>

  <!-- KPI Row -->
  <div class="kpi-row">
    <div class="kpi">
      <div class="kpi-label">Zielzeit</div>
      <div class="kpi-value" style="color:#60a5fa">{{ route.total_time_fmt }}</div>
      <div class="kpi-sub">Range {{ route.total_time_range }}</div>
    </div>
    <div class="kpi">
      <div class="kpi-label">Ziel-Ø Watt</div>
      <div class="kpi-value" style="color:#4ade80">{{ route.avg_power_w }}W</div>
      <div class="kpi-sub">IF {{ route.avg_if }} · ~{{ (route.avg_if * 100)|int }}% FTP</div>
    </div>
    <div class="kpi">
      <div class="kpi-label">W' verbraucht</div>
      <div class="kpi-value" style="color:#facc15">{{ route.w_prime_spent_kj }} kJ</div>
      <div class="kpi-sub">von {{ route.w_prime_total_kj }} kJ</div>
    </div>
    <div class="kpi">
      <div class="kpi-label">W' Reserve</div>
      <div class="kpi-value" style="color:{% if route.w_prime_remaining_kj < 5 %}#ef4444{% elif route.w_prime_remaining_kj < 10 %}#f97316{% else %}#4ade80{% endif %}">
        {{ route.w_prime_remaining_kj }} kJ
      </div>
      <div class="kpi-sub">{{ route.w_prime_pct_used }}% verplant</div>
    </div>
  </div>

  <!-- Profile + W' -->
  <div class="card">
    <div class="card-title">Höhenprofil · Leistungsverteilung</div>
    <div class="w-bar-wrap">
      <span style="font-size:0.7rem;color:#94a3b8;width:80px;flex-shrink:0">W' Balance</span>
      <div class="w-bar-track">
        <div class="w-bar-fill" style="width:{{ route.w_prime_pct_used }}%"></div>
      </div>
      <span style="font-size:0.7rem;font-weight:700;color:#4ade80;width:110px;flex-shrink:0;text-align:right">
        {{ route.w_prime_spent_kj }} / {{ route.w_prime_total_kj }} kJ
      </span>
    </div>
    {{ route.svg_profile | safe }}
    <div class="note">* Farben: Grün = aerob · Gelb = MIT/Schwelle · Orange = MIT+ · Rot = HIT/W'-Einsatz. Werte aus GPX + Physikmodell.</div>
  </div>

  <!-- Segment Table + Intuition -->
  <div class="two-col">
    <div class="card">
      <div class="card-title">Segmentplan</div>
      <table class="seg-table">
        <thead>
          <tr>
            <th>Segment</th>
            <th>Zone</th>
            <th>Watt</th>
            <th>km/h</th>
            <th>Split</th>
          </tr>
        </thead>
        <tbody>
          {% for seg in route.segments %}
          <tr>
            <td>
              <div class="seg-name">{{ seg.km_label }}</div>
              <div class="seg-sub">{{ seg.gradient_pct|round(1) }}% Gradient</div>
            </td>
            <td>
              <span class="badge" style="background:{{ seg.zone_color }}22;color:{{ seg.zone_color }}">
                {{ seg.zone_label }}
              </span>
            </td>
            <td style="font-weight:700;color:{{ seg.zone_color }}">{{ seg.target_w }}W</td>
            <td style="color:#94a3b8">{{ seg.speed_kmh }}</td>
            <td style="font-variant-numeric:tabular-nums;color:#94a3b8">{{ seg.time_fmt }}</td>
          </tr>
          {% endfor %}
          <tr>
            <td colspan="2"><strong>Gesamt · {{ route.total_dist_km }} km</strong></td>
            <td>Ø {{ route.avg_power_w }}W</td>
            <td>—</td>
            <td>{{ route.total_time_fmt }}</td>
          </tr>
        </tbody>
      </table>
    </div>

    <div class="card">
      <div class="card-title">Taktik & Intuition</div>
      <div class="tips">
        {% set icons = ["🚦", "⚠️", "🔋", "🌡️"] %}
        {% for tip in route.intuition %}
        <div class="tip">
          <div class="tip-icon">{{ icons[loop.index0 % 4] }}</div>
          <div class="tip-text">{{ tip }}</div>
        </div>
        {% endfor %}
      </div>
    </div>
  </div>

  {% if route.notes %}
  <div style="font-size:0.72rem;color:#64748b;margin-top:8px">📝 {{ route.notes }}</div>
  {% endif %}

</div>
{% endfor %}

<script>
function showTab(idx) {
  document.querySelectorAll('.tab').forEach((t,i) => t.classList.toggle('active', i===idx));
  document.querySelectorAll('.tab-content').forEach((c,i) => c.classList.toggle('active', i===idx));
}
</script>
</body>
</html>
```

- [ ] **Step 2: Commit**

```bash
git add pacing.template.html
git commit -m "feat: pacing.template.html mit Tab-Navigation, KPIs, Profil, Tabelle, Intuition"
```

---

## Task 7: Haupt-Render-Funktion + End-to-End-Test

**Files:**
- Modify: `generate_pacing.py` (main-Funktion anhängen)
- Modify: `tests/test_pacing.py` (End-to-End-Test)

- [ ] **Step 1: End-to-End Test schreiben**

An `tests/test_pacing.py` anhängen:
```python
from generate_pacing import render_pacing
import tempfile, os

def test_render_pacing_creates_html():
    """End-to-End: mini.gpx → HTML-Datei mit korrektem Inhalt."""
    with tempfile.NamedTemporaryFile(suffix=".html", delete=False) as f:
        out_path = f.name
    try:
        render_pacing(
            routes_dir="tests/fixtures",
            template_path="pacing.template.html",
            output_path=out_path,
        )
        html = open(out_path).read()
        assert "Race Pacing" in html
        assert "Zielzeit" in html
        assert "W' Balance" in html
        assert "Segment" in html or "km" in html
    finally:
        os.unlink(out_path)
```

- [ ] **Step 2: Test zum Scheitern bringen**

```bash
pytest tests/test_pacing.py::test_render_pacing_creates_html -v
```

Erwartet: `ImportError: cannot import name 'render_pacing'`

- [ ] **Step 3: `render_pacing` + `main` implementieren**

An `generate_pacing.py` anhängen:

```python
# ── Renderer + Entry Point ────────────────────────────────────────────────────

def render_pacing(
    routes_dir: str = "athlete/routes",
    template_path: str = "pacing.template.html",
    output_path: str = "docs/pacing.html",
) -> None:
    """Rendert alle aktiven GPX-Routen zu docs/pacing.html."""
    gpx_files = sorted(Path(routes_dir).glob("*.gpx"))
    if not gpx_files:
        print(f"[pacing] Keine GPX-Dateien in {routes_dir} gefunden — übersprungen.")
        return

    routes = []
    for gpx_path in gpx_files:
        try:
            ctx = build_route_context(str(gpx_path))
            routes.append(ctx)
            print(f"[pacing] {ctx['name']}: {ctx['total_time_fmt']} · {ctx['avg_power_w']}W")
        except Exception as e:
            print(f"[pacing] Fehler bei {gpx_path.name}: {e}")

    if not routes:
        return

    # Nach event_date sortieren (None ans Ende)
    routes.sort(key=lambda r: (r["event_date"] == "", r["event_date"] or "9999"))

    env = Environment(loader=FileSystemLoader("."), autoescape=False)
    template = env.get_template(template_path)
    html = template.render(routes=routes, generated_at=datetime.now().strftime("%d.%m.%Y %H:%M"))

    Path(output_path).parent.mkdir(parents=True, exist_ok=True)
    Path(output_path).write_text(html, encoding="utf-8")
    print(f"[pacing] → {output_path} ({len(routes)} Route(n))")


if __name__ == "__main__":
    render_pacing()
```

- [ ] **Step 4: Test mit Fixture-Ordner laufen lassen**

```bash
pytest tests/test_pacing.py::test_render_pacing_creates_html -v
```

Erwartet: PASSED

- [ ] **Step 5: Manuell mit echtem Ordner testen**

```bash
python generate_pacing.py
```

Erwartet (kein GPX in `athlete/routes/`): `[pacing] Keine GPX-Dateien in athlete/routes gefunden — übersprungen.`

- [ ] **Step 6: Alle Tests laufen lassen**

```bash
pytest tests/test_pacing.py -v
```

Erwartet: alle PASSED

- [ ] **Step 7: Commit**

```bash
git add generate_pacing.py
git commit -m "feat: render_pacing() main-Renderer, End-to-End komplett"
```

---

## Task 8: Dashboard Pacing-Karte

**Files:**
- Modify: `generate.py` (neue Funktion `build_pacing_card`)
- Modify: `dashboard.template.html` (neue Karte)

- [ ] **Step 1: `build_pacing_card` in `generate.py` implementieren**

Vor der `build_context`-Funktion in `generate.py` einfügen:

```python
def build_pacing_card(tage_bis_event: int) -> dict | None:
    """Gibt kompakte Pacing-Karte zurück wenn T-14 bis T-0 und GPX vorhanden.

    Liest die route mit dem nächstliegenden event_date aus athlete/routes/.
    """
    if tage_bis_event is None or tage_bis_event > 14 or tage_bis_event < 0:
        return None

    gpx_files = sorted(Path("athlete/routes").glob("*.gpx"))
    if not gpx_files:
        return None

    # Lade Route mit nächstem event_date
    from generate_pacing import load_route_meta, build_route_context
    today = date.today().isoformat()
    best_path = None
    best_date = "9999-12-31"
    for gpx_path in gpx_files:
        meta = load_route_meta(str(gpx_path))
        ed = meta.get("event_date") or "9999-12-31"
        if ed >= today and ed < best_date:
            best_date = ed
            best_path = gpx_path

    if best_path is None:
        best_path = gpx_files[0]  # Fallback: erste Route

    try:
        ctx = build_route_context(str(best_path))
    except Exception:
        return None

    # Steilstes Segment für Anker-Watt
    if not ctx["segments"]:
        return None
    steepest = max(ctx["segments"], key=lambda s: s["gradient_pct"])
    first    = ctx["segments"][0]

    return {
        "name":          ctx["name"],
        "event_date":    ctx["event_date"],
        "total_time":    ctx["total_time_fmt"],
        "avg_power":     ctx["avg_power_w"],
        "start_power":   first["target_w"],
        "peak_power":    steepest["target_w"],
        "peak_gradient": round(steepest["gradient_pct"], 1),
        "w_prime_pct":   ctx["w_prime_pct_used"],
    }
```

- [ ] **Step 2: `pacing_card` in `build_context()` Rückgabe eintragen**

Im Return-Dict von `build_context()` (am Ende der Funktion) ergänzen:

```python
        "pacing_card": build_pacing_card(tage_bis_event),
```

`tage_bis_event` wird in `build_context()` bereits berechnet (aus `RACE_KW` und heutigem Datum). Falls noch nicht vorhanden:
```python
        race_monday, _ = week_date_range(RACE_KW, _current_year)
        tage_bis_event = (race_monday - date.today()).days
```

- [ ] **Step 3: Pacing-Karte in `dashboard.template.html` einfügen**

Nach dem bestehenden Ernährungs-Karten-Block (suche `<!-- ERNÄHRUNG -->`) einen neuen Block einfügen:

```html
  <!-- PACING (nur T-14 bis T-0) -->
  {% if pacing_card %}
  <div class="nutr-card" style="margin-bottom:0">
    <div style="display:flex;align-items:center;gap:10px;margin-bottom:12px">
      <div class="chard" style="margin-bottom:0">
        <div class="chard-icon" style="background:rgba(96,165,250,0.14)">⚡</div>
        Pacing · {{ pacing_card.name }}
      </div>
      {% if pacing_card.event_date %}
      <span class="kw-pill" style="margin-left:auto">{{ pacing_card.event_date }}</span>
      {% endif %}
    </div>
    <div style="display:grid;grid-template-columns:1fr 1fr;gap:10px;margin-bottom:12px">
      <div style="background:var(--surface2);border-radius:8px;padding:10px 12px">
        <div style="font-size:0.6rem;color:var(--muted);margin-bottom:3px">ZIELZEIT</div>
        <div style="font-size:1.2rem;font-weight:800;color:#60a5fa">{{ pacing_card.total_time }}</div>
      </div>
      <div style="background:var(--surface2);border-radius:8px;padding:10px 12px">
        <div style="font-size:0.6rem;color:var(--muted);margin-bottom:3px">Ø ZIELWATT</div>
        <div style="font-size:1.2rem;font-weight:800;color:#4ade80">{{ pacing_card.avg_power }}W</div>
      </div>
    </div>
    <div style="font-size:0.75rem;color:var(--muted);line-height:1.8">
      🚦 Start: <strong style="color:#4ade80">{{ pacing_card.start_power }}W</strong> — locker beginnen<br>
      ⚠️ Steilstück ({{ pacing_card.peak_gradient }}%): <strong style="color:#f97316">{{ pacing_card.peak_power }}W</strong> max.<br>
      🔋 W' Budget: <strong>{{ pacing_card.w_prime_pct }}%</strong> verplant
    </div>
    <a href="pacing.html" style="display:block;margin-top:12px;text-align:center;font-size:0.7rem;color:#60a5fa;text-decoration:none;padding:6px;background:rgba(96,165,250,0.08);border-radius:6px">
      → Vollanalyse öffnen
    </a>
  </div>
  {% endif %}
```

- [ ] **Step 4: Syntax prüfen**

```bash
python3 -c "import ast; ast.parse(open('generate.py').read()); print('OK')"
```

Erwartet: `OK`

- [ ] **Step 5: Commit**

```bash
git add generate.py dashboard.template.html
git commit -m "feat: Dashboard Pacing-Karte (T-14 bis T-0) mit Deep-Link zu pacing.html"
```

---

## Task 9: Coach-Skill Pacing-Block

**Files:**
- Modify: `.claude/skills/coach/SKILL.md`

Kein Code-Test. Textuell validiert durch Lesen.

- [ ] **Step 1: Rennwoche-Modus im Skill erweitern**

In `.claude/skills/coach/SKILL.md` den Abschnitt `### Rennwoche-Modus` suchen (Stichwort: `T-6 bis T-4`) und **davor** folgenden Block einfügen:

```markdown
### Pacing-Block (Rennwoche-Modus und manueller Trigger)

**Wann ausgeben:**
- Automatisch: wenn `tage_bis_event ≤ 7` und eine GPX-Route mit passendem `event_date` in `athlete/routes/` liegt
- Manuell: wenn Stefan schreibt „pacing für [Route]", „wie pace ich [Strecke]", „gib mir Watt-Ziele für [Event]"

**Datenbasis:** `generate_pacing.py` Logik — lies GPX + `athlete/profil.md` + `athlete/fortschritt.md`. Für den Coach-Skill-Output die Berechnung direkt inline durchführen (keine externe Datei nötig):
1. Lade `athlete/routes/<passende>.gpx` + optionale `.json`
2. Lese FTP/CP aus `athlete/profil.md`, W' aus `athlete/fortschritt.md`
3. Berechne Segmente mit `assign_power` Logik (Gradient-Tabelle × CP)
4. Berechne Gesamtzeit via Physikmodell
5. Gib Anker-Wattwerte + Intuitionssatz pro kritischem Segment aus

**Format:**

```
🏁 Pacing · [Route-Name] · [Datum wenn vorhanden]

Zielzeit: [MM:SS] (Range [MM:SS]–[MM:SS]) · Ø [X]W · IF [Y]
W' Budget: [X] von [Y] kJ verplant · [Z] kJ Reserve [✅/⚠️]

Anker-Wattziele:
  [km X,x–Y,y] ([grad]%) → [W]W  — [ein Satz Taktik]
  [km X,x–Y,y] ([grad]%) → [W]W  ⚠ [ein Satz wenn kritisch]
  ...

Intuition: "[ein konkreter Satz was sich das körperlich anfühlt]"

→ Vollanalyse: https://hallo-jpg.github.io/coaching-dashboard/pacing.html
```

**Für Gran-Fondo / Rennen (>60km):** Zusätzlich Energie-Budget ausgeben:
```
Energie-Budget: ~[kcal] über [Dauer] · [X]g KH/h empfohlen
```

**Wenn keine GPX-Datei vorhanden:** Pacing aus bekannten Eckdaten berechnen (Distanz, Gesamt-Hm, Ø Gradient aus `periodisierung.md` oder Nutzer-Angabe). Hinweis: „Pacing basiert auf Strecken-Eckdaten, nicht GPX — nach GPX-Upload wird präziser."
```

- [ ] **Step 2: Lesen und prüfen**

```bash
grep -A 5 "Pacing-Block" ".claude/skills/coach/SKILL.md" | head -10
```

Erwartet: Block ist sichtbar.

- [ ] **Step 3: Commit + Push**

```bash
git add ".claude/skills/coach/SKILL.md"
git commit -m "feat: Coach-Skill Pacing-Block für Rennwoche und manuellen Trigger"
git pull --rebase && git push
```

---

## Self-Review

**Spec Coverage:**
- ✅ GPX-Parser + 50m-Resampling: Task 2
- ✅ Physikmodell (Newton-Solver): Task 3
- ✅ W'-Balance (Verbrauch + Rekonstituierung): Task 3
- ✅ Segment-Power-Zuweisung (Gradient-Tabelle × Typ-Faktor): Task 3
- ✅ Intuitionstext-Generator: Task 5
- ✅ `athlete/routes/` + `archiv/`: Task 1
- ✅ Optionale JSON-Metadaten: Task 4 (`load_route_meta`)
- ✅ `pacing.html` mit Tab-Navigation: Tasks 6+7
- ✅ SVG-Profil ohne Textverzerrung: Task 5 (`_build_svg_profile`, Text außerhalb scaled area)
- ✅ `build_pacing_card` (T-14 bis T-0): Task 8
- ✅ Coach-Skill Pacing-Block: Task 9
- ✅ `build.yml` Pipeline: Task 1
- ✅ `gpxpy` Dependency: Task 1
- ✅ Tab-Reihenfolge nach `event_date`: Task 7 (`routes.sort`)
- ✅ Archivieren = Datei verschieben → automatisch aus Tab entfernt (kein extra Code)

**Placeholder-Scan:** Keine TBDs oder TODOs.

**Typ-Konsistenz:**
- `parse_gpx` → `list[dict]` mit keys `dist`, `ele` → konsistent in `group_segments` ✅
- `group_segments` → `list[dict]` mit `dist_m`, `gradient_pct`, `elev_start`, `elev_end` → konsistent in `compute_route` ✅
- `compute_route` → ergänzt `target_w`, `time_s`, `speed_kmh`, `km_start`, `km_end`, `km_label`, `zone_label`, `zone_color` → alle im Template verwendet ✅
- `compute_w_prime_balance` → ergänzt `w_prime_balance_j` → konsistent in `build_route_context` ✅
- `build_pacing_card` importiert `load_route_meta` + `build_route_context` aus `generate_pacing` ✅
